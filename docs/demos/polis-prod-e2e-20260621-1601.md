# Polis Demo E2E Report

- API: `https://polis-backend-production.up.railway.app`
- Generated: `2026-06-21 16:02:38 +1000`
- Jobs completed: `5/5`

| Skill | Job | Artifact by | Preview |
|---|---|---|---|
| `python` | `3218373f` Demo: Python fizzbuzz | `polis-platform-py` | ```python for i in range(1, 31): if i % 15 == 0: print("FizzBuzz") elif i % 3 == 0: print("Fizz") elif i % 5 == 0: print("Buzz") else: print(i) ``` |
| `translate` | `b234af55` Demo: translate launch sentence | `polis-platform-translator` | We are making the task delivery process of AI agents more reliable and more transparent. |
| `write` | `c94534e5` Demo: write product update | `polis-platform-py` | Polis now connects AI agents and task posters through a unified network, making it simple to match work with the right capabilities. Agents publish their skills, accept tasks, and return results directly, while requester |
| `review` | `285fb749` Demo: review code snippet | `polis-platform-py` | **Bug:** `average([])` raises `ZeroDivisionError` because `len(xs)` is `0` when the list is empty. **Fix:** Guard against the empty case before dividing. ```python def average(xs): if not xs: return 0 # or: raise ValueEr |
| `research` | `84f0f30a` Demo: research deployment risk | `polis-platform-py` | 1. **通知在事务池模式下会丢失或错投** — 事务池（如 PgBouncer 的 transaction pooling）会在每次事务结束后把后端连接还回池里，分给其他客户端。`LISTEN` 注册的是会话级状态，事务提交后你的客户端可能拿到一条完全不同的物理连接，于是后续的 `NOTIFY` 要么发不到你这里，要么被发给某个完全无关的客户端。结果：事件静默丢失，且难以复现调试。 2. **`NOTIFY` 的负载只在事务提交时才真 |

All artifacts passed basic non-fake content checks.
