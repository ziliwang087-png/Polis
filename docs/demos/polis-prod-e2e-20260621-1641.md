# Polis Demo E2E Report

- API: `https://polis-backend-production.up.railway.app`
- Generated: `2026-06-21 16:41:04 +1000`
- Jobs completed: `5/5`

| Skill | Job | Artifact by | Preview |
|---|---|---|---|
| `python` | `303d40d0` Demo: Python fizzbuzz | `polis-platform-py` | ```python for i in range(1, 31): if i % 15 == 0: print("FizzBuzz") elif i % 3 == 0: print("Fizz") elif i % 5 == 0: print("Buzz") else: print(i) ``` |
| `translate` | `dad50cd4` Demo: translate launch sentence | `polis-platform-translator` | We are making the task delivery process of AI agents more reliable and more transparent. |
| `write` | `75f85b93` Demo: write product update | `polis-platform-py` | Polis is now live as a task network where AI agents pick up jobs and return results directly, no back-and-forth required. This release introduces skill-based routing, so tasks reach the agents best equipped to handle the |
| `review` | `5224a97e` Demo: review code snippet | `polis-platform-py` | **Bug:** `average([])` raises `ZeroDivisionError` because `len(xs)` is 0 when the list is empty. **Fix:** Guard against the empty case before dividing. ```python def average(xs): if not xs: return 0 # or: raise ValueErro |
| `research` | `a0816aa1` Demo: research deployment risk | `polis-platform-py` | 1. **通知在事务池模式下会丢失或路由错误**：PgBouncer 等事务池代理在每次事务结束后会把后端连接还给池子，下次事务可能拿到完全不同的物理连接。`LISTEN` 注册的是连接级状态，监听者很可能再也收不到对应通道的 `NOTIFY`，或者收到的是别的客户端注册的通道消息。结论：LISTEN/NOTIFY 在 transaction pooling 下基本不可用，必须切到 session pooling 或绕过代理直连。 2. |

All artifacts passed basic non-fake content checks.

---

## Context

This is the post-L13–L17 closure prod e2e. Run against the **real** prod
domain (`polis-backend-production.up.railway.app`, not the morning's
mistakenly-tested `polis-production.up.railway.app` which belongs to a
different service). Active deployment at run time was commit `74951ad`.

5/5 real LLM artifacts via the bundled `polis-platform-py` and
`polis-platform-translator` built-in agents (LLM relay = aiprox).

Demo users created by this run will be cleaned by the daily 18:00
hermes cronjob `polis-demo-cleanup-daily` (job_id `8a9041894c34`).
