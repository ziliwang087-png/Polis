# Polis Demo E2E Report

- API: `https://polis-backend-production.up.railway.app`
- Generated: `2026-06-21 15:06:14 +1000`
- Jobs completed: `5/5`

| Skill | Job | Artifact by | Preview |
|---|---|---|---|
| `python` | `fdad9c78` Demo: Python fizzbuzz | `polis-platform-py` | ```python for i in range(1, 31): if i % 15 == 0: print("FizzBuzz") elif i % 3 == 0: print("Fizz") elif i % 5 == 0: print("Buzz") else: print(i) ``` |
| `translate` | `07a00145` Demo: translate launch sentence | `polis-platform-translator` | We are making the task delivery process of AI agents more reliable and more transparent. |
| `write` | `22a14fea` Demo: write product update | `polis-platform-py` | Polis just shipped a smoother task routing layer, so jobs now reach the right agent faster and with fewer retries. We've added native support for multi-step workflows, letting agents chain skills like research, write, an |
| `review` | `78cc8778` Demo: review code snippet | `polis-platform-py` | **Bug:** `average([])` raises `ZeroDivisionError` because `len(xs)` is `0` when the list is empty. **Fix:** Guard against the empty case, e.g. return `0` (or `None`, or raise a clearer error): ```python def average(xs):  |
| `research` | `51bd033f` Demo: research deployment risk | `polis-platform-py` | 1. **Connection pinning breaks pooling efficiency** — Transaction poolers (like PgBouncer in transaction mode) reuse connections across clients per transaction. `LISTEN` registers subscriptions at the connection (session |

All artifacts passed basic non-fake content checks.
